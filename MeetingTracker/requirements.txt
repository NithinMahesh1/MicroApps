# MeetingTracker — Google Calendar / Gmail TUI dependencies.
# Pinned to the installed, pip-audit-clean versions (matching the repo's
# pinning discipline used by the other apps' requirements files).
google-api-python-client==2.190.0
google-auth-oauthlib==1.2.4
google-auth-httplib2==0.3.0
rich==14.3.3
pyfiglet==1.0.4
